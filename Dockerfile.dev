# Dev image with hot reload — source is bind-mounted from the host.
FROM node:20-alpine
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci --no-audit --no-fund
ENV NEXT_TELEMETRY_DISABLED=1
ENV PORT=3000
EXPOSE 3000
CMD ["npm", "run", "dev", "--", "-H", "0.0.0.0"]
